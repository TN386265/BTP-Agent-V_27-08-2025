projection;
strict ( 1 );

define behavior for /ZUORA001/C_BL_H //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association CONDITIONDATAIN { create; }
  use association ERRORS { create; }
  use association BILLINGDATAIN { create; }
  use association RETURN { create; }
  use association NFMETALLITMS { create; }
  use association SUCCESS { create; }
  use association TEXTDATAIN { create; }
  use association CCARDDATAIN { create; }
}

define behavior for /ZUORA001/C_BL_C //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}

define behavior for /ZUORA001/C_BL_E //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}

define behavior for /ZUORA001/C_BL_I //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;

}



define behavior for /ZUORA001/C_BL_R //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}


define behavior for /ZUORA001/C_BL_N //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}



define behavior for /ZUORA001/C_BL_S //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}

define behavior for /ZUORA001/C_BL_T //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}

define behavior for /ZUORA001/C_BL_V //alias <alias_name>
{
  use update;
  use delete;

  use association CREATORDATAIN;
}